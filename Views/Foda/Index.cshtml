@model IEnumerable<Proyecto_Graduacion.Models.fodaaudit>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.InfoFortaleza)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.InfoOportunidad)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.InfoDebilidad)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.InfoAmenaza)
            </th>
            <th>
                Auditoria
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.InfoFortaleza)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.InfoOportunidad)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.InfoDebilidad)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.InfoAmenaza)
            </td>
            <td>
                    @Html.DisplayFor(modelItem => item.Auditoria)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdFoda">Edit</a> |
                <a asp-action="Delete" asp-route-id="@item.IdFoda">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
