@model IEnumerable<Proyecto_Graduacion.Models.MaterialidadViewModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Monto)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Auditoria)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Monto)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Auditoria)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdMaterialidad">Edit</a> |
                <a asp-action="Delete" asp-route-id="@item.IdMaterialidad">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
